const TodoTitle: React.FC = () => {
  return (
    <div className='todoTitle'>
      <p>Let's add what you have to do!</p>
      <p>
        Fill the input and click the button or press Enter to add a new task into the list.
        <br />
        To mark as completed, just click directly on the task.
      </p>
    </div>
  )
}

export default TodoTitle
